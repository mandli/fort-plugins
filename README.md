# Simple Fortran Plugin Tests

Code is meant as a simple test example to compare
1. pre-processor directives,
2. function pointers, and
3. if-statements

as means for providing plug-in like capabilities in Fortran.
